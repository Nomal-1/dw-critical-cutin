# 프로젝트 개요

Foundry VTT (FVTT) v12.331 환경에서 사용하는 모듈과 매크로를 개발하는 프로젝트입니다.

- **대상 시스템/캠페인**: Dungeon World
- **FVTT 버전**: v12.331
- **선호 언어**: JavaScript

# 버전 호환성 주의사항

- 이 프로젝트는 **FVTT v12.331 기준**으로 개발한다
- 최신 FVTT는 v13까지 나와있고, v12와 v13 사이에 API가 달라진 부분(예: ApplicationV2, 새로운 Hooks 이름/시그니처 등)이 있을 수 있다
- 코드를 작성할 때는 **v12.331 기준 API를 우선 사용**할 것
- 만약 v13 이상에서만 동작하는 최신 문법(ApplicationV2, 새로운 Hooks 등)을 사용하게 되는 경우, 반드시 그 사실을 먼저 알리고 v12 호환 대안이 있는지 확인할 것
- module.json의 `compatibility` 필드에는 v12 기준으로 minimum/verified 버전을 명시할 것

# 작업 목적

주로 아래 세 가지 작업을 다룹니다.

1. **트러블슈팅**: 기존 모듈/매크로에서 발생하는 오류나 예상과 다른 동작 원인 파악 및 수정
2. **신규 모듈 제작**: FVTT 모듈 구조(module.json, 스크립트, 훅 등)를 새로 만드는 작업
3. **매크로 작성**: Dungeon World 캠페인 진행에 필요한 매크로 스크립트 작성

# 코딩 설명 방식

사용자는 코딩 초보입니다. 코드를 작성하거나 수정할 때 다음을 지켜주세요.

- 코드만 던지지 말고, 무엇을 왜 그렇게 작성했는지 함께 설명할 것
- FVTT/Foundry 고유 개념(훅, API, 시스템 데이터 구조 등)이 나오면 간단히 짚어줄 것
- 전문 용어는 풀어서 설명하고, 너무 축약된 설명은 피할 것

# 모듈 명세: 대성공 컷인 (dw-critical-cutin)

던전월드(Dungeon World)에서 정해진 발동 조건을 만족하면, 전체 화면 컷인 연출을 자동으로 재생하는 모듈.
저장소: https://github.com/Nomal-1/dw-critical-cutin

## 트리거

수동 트리거(버튼, 매크로 실행 등)는 없음. `createChatMessage` 훅에서 던전월드의 채팅 카드(`message.content` HTML)를 파싱해 아래 조건 중 하나라도 만족하면 발동. 모든 조건은 GM 전용 world 설정(Configure Settings)에서 켜고 끌 수 있음.

- **플레이어 대성공**: PC의 무브 판정(2d6)이 성공(합계 10 이상, `.result.success`)일 때 (기본 켜짐)
- **플레이어 성공**: PC의 무브 판정이 부분 성공(합계 7~9, `.result.partial`)일 때 (기본 꺼짐)
- **플레이어 데미지 초과**: PC의 데미지 굴림(`.chat-damage-buttons` 존재) 합계가 설정한 기준값을 넘을 때 (기준값 0 = 사용 안 함)
- **몬스터 데미지 초과**: NPC의 데미지 굴림 합계가 설정한 기준값을 넘을 때 (기준값 0 = 사용 안 함)
- **몬스터 액션 사용**: 특정 NPC 액션(npcMove) 아이템 시트에 있는 "이 액션 사용 시 컷인 발동" 체크박스가 켜져 있으면, 주사위 결과·판정 여부와 무관하게 그 액션이 사용될 때마다 발동

던전월드 시스템은 채팅 카드 템플릿(`chat-move.html`) 하나를 무브 판정/데미지 굴림/굴림 없는 액션 사용에 공통으로 재사용하고, `ChatMessage` 문서 자체(`message.rolls`)에는 굴림 결과를 저장하지 않으므로, 렌더링된 HTML을 파싱하는 방법이 유일함. 카드 종류 구분:
- 무브 판정: `resultLabel`/`result`가 계산되어 `.result.success`/`.result.partial`/`.result.failure` 클래스가 붙음
- 데미지 굴림: `result` 클래스 대신 `.chat-damage-buttons`(피해 적용 버튼 묶음)가 붙고 `data-roll-total`에 합계가 담김
- 굴림 없는 액션 사용: 카드 제목(`.cell__title`)에 그 액션 아이템의 이름이 그대로 표시됨 (아이템 ID는 카드에 없어서, 이름 일치로 어떤 액션인지 식별함 — 같은 이름의 액션이 여러 개면 구분 못하는 한계 있음)

## 캐릭터별 설정

- PC/NPC 액터 모두 컷인에 쓸 이미지와 효과음을 개별로 지정 (이미지는 필수, 효과음은 선택)
- 액터 시트 헤더의 "컷인 설정" 버튼(GM에게만 보임) → FilePicker로 이미지/효과음 선택 → 액터 flag(`actor.flags['dw-critical-cutin']`)에 저장
- 이 설정 버튼은 `game.user.isGM` 체크로 플레이어에게는 보이지 않음 (자기 캐릭터 시트를 열어도 안 보임)

## 재생 방식

- 트리거되면 전체 화면 오버레이로 지정된 이미지를 표시하고 효과음 재생 (`AudioHelper.play`, 인터페이스 볼륨 채널 사용)
- 연출은 "등장(확대) → 유지 → 페이드아웃" 3단계 순차 재생이며, 각 단계 시간(ms)은 GM 전용 world 설정에서 개별 조정 가능. 값이 0이면 그 단계를 애니메이션 없이 스킵
- 컷인이 떠 있는 동안은 클릭이 막히고(포인터 이벤트 차단), 페이드아웃이 끝나 오버레이가 사라지면 다시 조작 가능
- 연출은 GM뿐 아니라 접속한 모든 플레이어 화면에 동시에 재생됨. 별도의 `game.socket` 브로드캐스트는 쓰지 않는데, Foundry가 새 `ChatMessage`를 모든 클라이언트에 자동 동기화하므로 `createChatMessage` 훅이 각 클라이언트에서 알아서 동시에 실행되기 때문

## 이번 버전 범위 제외 (Out of scope)

- 여러 개의 테마 지원
- 리소스 소모/컨디션 발생 등 판정·데미지·특정 액션 사용 외의 트리거
- 파티원 동시 공격 등 다중 캐릭터 연출
